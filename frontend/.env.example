# Base URL del backend Go.
# En dev local apunta al puerto 8080. En producción suele ser un origen
# vacío o relativo si servís front+back detrás del mismo reverse proxy.
VITE_API_BASE_URL=http://localhost:8080
