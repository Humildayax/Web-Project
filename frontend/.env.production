# En producción el front se sirve detrás del mismo origen que la API
# (nginx hace el proxy). Por eso BASE_URL queda vacío y los fetch
# usan rutas relativas: fetch('/api/news') -> nginx -> backend:8080.
VITE_API_BASE_URL=
